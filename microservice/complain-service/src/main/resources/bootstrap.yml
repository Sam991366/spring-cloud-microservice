spring:
  application:
    name: complain-service

  cloud:
    config:
      uri: http://localhost:8888      # Config Server URL
      profile: dev                    # or qa, staging, prod (via --spring.profiles.active)
